require 'rails_helper'

RSpec.describe LocationRegister, type: :model do
  prefix = "LocationRegister :: "
  context "with 2 or more comments" do
    it "insert location_register" do
      # longitude = 88.0341339111328
      # latitude = 22.8135295119532
      # user_id = 1
      # # Call for insert new entry using 
      # actual = LocationRegister.insert_location_data(latitude, longitude, user_id)
      # # Now check directly to check the latest result is same or not from Location register
      # expected = LocationRegister.last.id
      # expect(actual).to be(expected)
      
      
    end
  end
end
